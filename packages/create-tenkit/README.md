# create-tenkit

Thin package-manager create entrypoint for Tenkit.

This package delegates to `@tenkit/cli` and does not own prompts, Template generation,
dependency installation, git policy, or output formatting.
